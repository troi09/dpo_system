Be smart in implementing these to the code base. Ask yourself: "would a staff engineer approve this". run test, check logs, demonstrate correctness.
For non-trivial changes: pause and ask "is there a more elegant way?" skip this for simple, chvious fixes - dont over engineer. Challenge your own work before presenting it.
Simplicity first: make every chnage as simple as possible. impact minimal code.
No laziness: Find root causes. No temporary fixes. Senior developer standards.
Minimal impact: changes hould only touch what's necessary. avoid introducing bugs.

Notes
-Both cater the changes and the requirements to be able to not just run on the local, but also on vercel as a deployed websystem
-Check the whole system if there are requiements need to be changed or replace for being able to run in vercel (include .env files, since its in gitignore and the changes would need be maually added)
-At the end of this, Make a read me or any explnation that makes me instructions or guides for me to implement, to do, to replace, to install depndencies, or any requirement at all after implementing the changes

in the login page Make the logo and titles of DPO adn RTU larger by around 20%
also in the login page, when viewed in phone size, make the background image fill the background, since when in phne view theres a big gap on the bottom, the background doest fill the entire screen. Also lock the login screen so it is not scrollable
when the user creates a request and picks the NDA option, make the option up next (school org activities, and conduct research) the same buttons layout and buttons as the initial pciking of NDA and Agreeement, and below the logo and titel of the form, a brief explnation of what si the purpose of both (for what it is)
in the request review page, make  the page form centered and make it wider to fit the progress bar tracking 

general bug. when the websystem is deployed to vercel already same code and all. When we load in the dashbaord the datas are empty, the responsive grid at the top have no data (like total request, pending, etc. are 0), the data charts and graphs are empty (although the data of these charts and graph are available in the reports and analytics page, just the daashabord are mising data)
similar to other issue. Audit logs are not avaiable and not retreieving in vercel deployment.
same also with the users management tab, tehre are no users loaded in and gives the error of Failed to load users
all of these error and bugs happens in the vercel deployment mode, but local it all works flawlessly